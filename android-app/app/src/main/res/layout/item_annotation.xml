<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?selectableItemBackground"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Header Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/annotationIcon"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginEnd="8dp"
                android:src="@drawable/ic_highlight"
                android:tint="@color/bahai_secondary" />

            <TextView
                android:id="@+id/annotationTypeLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Highlight"
                android:textColor="@color/bahai_secondary"
                android:textSize="12sp"
                android:textStyle="bold" />

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/pageNumber"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/rounded_background"
                android:backgroundTint="@color/bahai_secondary"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:text="Page 1"
                android:textColor="@color/bahai_on_secondary"
                android:textSize="10sp" />

        </LinearLayout>

        <!-- Selected Text -->
        <TextView
            android:id="@+id/selectedText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/highlight_background"
            android:padding="8dp"
            android:text="Selected text that was highlighted..."
            android:textColor="@color/bahai_on_surface"
            android:textSize="14sp"
            android:textStyle="italic" />

        <!-- Notes (if any) -->
        <TextView
            android:id="@+id/annotationNote"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/note_background"
            android:padding="8dp"
            android:text="Personal note about this text..."
            android:textColor="@color/bahai_on_surface"
            android:textSize="13sp"
            android:visibility="gone" />

        <!-- Action Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/annotationDate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Jan 15, 14:30"
                android:textColor="@color/bahai_on_surface_variant"
                android:textSize="11sp" />

            <ImageButton
                android:id="@+id/editButton"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginEnd="8dp"
                android:background="?selectableItemBackgroundBorderless"
                android:src="@drawable/ic_edit"
                android:tint="@color/bahai_on_surface_variant"
                android:contentDescription="Edit annotation" />

            <ImageButton
                android:id="@+id/deleteButton"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?selectableItemBackgroundBorderless"
                android:src="@drawable/ic_delete"
                android:tint="@color/bahai_on_surface_variant"
                android:contentDescription="Delete annotation" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>